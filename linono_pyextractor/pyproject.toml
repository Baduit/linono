[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "linono_pyextractor"
description = "Python bindings for linono_extractor"
version = "0.1.0"
dependencies = []
